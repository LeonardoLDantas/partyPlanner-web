{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Party Planner Web (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "partyplanner-web: dev server"
    }
  ]
}
